{
  "name": "fullstack-step-by-step",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "format": "prettier --ignore-unknown --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
